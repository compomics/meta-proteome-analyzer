package de.mpa.task;

/**
 * Search type enum: target/decoy or first round (iterative) database search.
 * 
 * @author T. Muth
 *
 */
public enum SearchType {
	TARGET, DECOY, TARGET_DECOY, FIRSTROUND
}
